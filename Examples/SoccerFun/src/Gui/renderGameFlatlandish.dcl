definition module renderGameFlatlandish

/** Render a football match in Flatland-style:
		the camera hovers above the center of the football field and everything is displayed in a
		deliberately simplistic manner.
*/
import StdIOCommon, StdPSt
import matchControl, render

/**	renderFlatland
		renders a complete match in Flatland style.
*/
renderFlatland :: RenderStyle
